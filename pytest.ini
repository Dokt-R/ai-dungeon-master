[pytest]
addopts =
    --ignore=packages/backend/tests/test_transcript_logger.py
    # ; -p no:sugar
    # --cov=packages           # measure coverage for your "packages" folder
    # --cov-report=term-missing
    # --cov-report=html
    # --cov-report=xml
    # -n auto
testpaths = 
    tests
filterwarnings = 
    ignore:.*datetime.datetime.utcnow\(\) is deprecated.*
pythonpath = .